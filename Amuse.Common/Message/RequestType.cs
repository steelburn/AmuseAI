namespace Amuse.Common.Message
{
    public enum RequestType
    {
        Start = 0,
        Stop = 1,
        Create = 10,
        Load = 11,
        Reload = 12,
        Unload = 13,
        Run = 20
    }
}
